# === CONFIG ===

NAME        := libft.a
CC          := cc
CFLAGS      := -Wall -Wextra -Werror -g -fsanitize=undefined
AR          := ar rcs
RM          := rm -f
MKDIR_P     := mkdir -p
INCLUDES    := -Iincludes
